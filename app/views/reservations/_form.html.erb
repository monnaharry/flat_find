<div class="date-form date">
  <%= simple_form_for [@flat, @reservation] do |f| %>
    <%= f.input :start_date, as: :date, html5: true %>
    <%= f.input :end_date, as: :date, html5: true %>
    <%= f.submit class: 'btn-primary-teal'%>
  <% end %>
</div>
