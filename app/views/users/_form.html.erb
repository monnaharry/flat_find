<%= simple_form_for(@user)  do |f| %>
  <%= f.button :submit, 'Update Profile Picture', class: 'btn btn-secondary-teal'  %>
  <%= f.input :profile_pic, label: false %>
  <%= f.input :profile_pic_cache, as: :hidden %>
<% end %>
